# Server Configuration
PORT=3003
NODE_ENV=development

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/bia_db
DATABASE_URL_REPLICA=postgresql://user:password@localhost:5432/bia_db

# Security
JWT_SECRET=your_super_secret_jwt_key_at_least_32_chars
ENCRYPTION_KEY=your_aes_256_hex_key_64_chars
INITIAL_ADMIN_PASSWORD=your_admin_password
INITIAL_TRADING_ADMIN_PASSWORD=your_trading_admin_password
DEFAULT_SUPERADMIN_EMAIL=contact@cambobia.com
DEFAULT_TRADING_SUPERADMIN_EMAIL=trading-admin@cambobia.com
COOKIE_SECRET=your_cookie_secret_at_least_32_chars
CSRF_SECRET=your_csrf_secret_at_least_32_chars
SSO_INTERNAL_API_KEY=your_sso_internal_api_key_at_least_32_chars

# Platform mode: core | trading
SERVICE_MODE=core

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
TRADING_FRONTEND_URL=http://localhost:3006
# Required in trading mode (secure backchannel to core service):
CORE_SSO_CONSUME_URL=http://backend.railway.internal:8080/api/auth/sso/trading/consume
# Keep false in production; true only for temporary migration compatibility.
ALLOW_CROSS_PLATFORM_CORS=false

# Email Service (Resend)
RESEND_API_KEY=re_your_api_key
EMAIL_FROM=contact@cambobia.com

# File Storage (AWS S3 or Google Cloud Storage)
STORAGE_PROVIDER=GCS # or S3
STORAGE_BUCKET_NAME=your_bucket_name

# If using GCS:
# Either the path to a JSON file:
GOOGLE_APPLICATION_CREDENTIALS=path/to/key.json
# OR the JSON content itself (useful for Railway):
GCP_SERVICE_ACCOUNT_JSON={"project_id": "...", ...}
# Public URL for direct access if needed:
STORAGE_PUBLIC_BASE_URL=https://storage.googleapis.com/your_bucket_name

# If using S3/R2:
S3_REGION=auto
S3_ENDPOINT=your_s3_endpoint
S3_ACCESS_KEY_ID=your_access_key
S3_SECRET_ACCESS_KEY=your_secret_key
S3_PUBLIC_URL=your_public_s3_url

# ABA PayWay Integration
ABA_PAYWAY_API_URL=https://checkout-sandbox.payway.com.kh/api/payment-gateway/v1/payments/purchase
ABA_PAYWAY_API_KEY=your_aba_api_key
ABA_PAYWAY_MERCHANT_ID=your_merchant_id
ABA_PAYWAY_CALLBACK_URL=https://your-domain.com/api/webhooks/aba

# Sumsub KYC
SUMSUB_APP_TOKEN=your_sumsub_token
SUMSUB_SECRET_KEY=your_sumsub_secret
SUMSUB_LEVEL_NAME=basic-kyc-level

# Stripe (Optional for testing, required for real payments)
STRIPE_SECRET_KEY=sk_test_your_key
